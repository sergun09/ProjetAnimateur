{% extends 'base.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite {{activite.Nom}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ activite.Nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('activite_index') }}">back to list</a>

    <a class="btn btn-warning ml-2" href="{{ path('activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('activite/_delete_form.html.twig') }}
{% endblock %}
